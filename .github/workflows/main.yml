on:
   push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Отправление лабораторной работы на проверку
        run: |
         pip install requests
         ls -lR
         python3 .workflow/connect.py "Informatics" "1"
